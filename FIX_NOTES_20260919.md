# FIX 2026-09-19 — Voice + AI Chat

Đã sửa:

1. AIChat không còn gửi payload lồng `{ message: { message: ... } }`.
2. API client và server cùng normalize message để tương thích cả string lẫn object cũ.
3. AI chatbot luôn có local fallback nếu Gemini lỗi key/quota/model/network; không làm panel chết.
4. SpeechRecognition realtime dùng `resultIndex`, tách final/interim và render trực tiếp vào ô Nội dung.
5. SpeechRecognition tự khởi động lại khi Chrome tự kết thúc phiên trong lúc vẫn đang thu âm.
6. Khi bấm Dừng, chỉ gọi audio transcription fallback nếu browser chưa nhận được chữ.
7. Thêm console log `[VOICE]`, `[AI]`, `[AI AUDIO]` để dò lỗi rõ ràng.
8. Normalize MIME `audio/webm;codecs=opus` -> `audio/webm` trước khi gửi Gemini.
9. Tăng giới hạn JSON server lên 12 MB để payload base64 không sát trần 8 MB.

Kiểm tra đã chạy:
- `node --check` server: OK.
- Parse JSX bằng `@babel/parser`: OK.
- `npm run build` trong môi trường sandbox không chạy do thiếu optional native package `@rollup/rollup-linux-x64-gnu` của Rollup; đây là dependency môi trường, không phải lỗi JSX.
