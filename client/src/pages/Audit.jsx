import { useEffect,useState } from 'react';import { api } from '../services/api';
export default function Audit(){const [rows,setRows]=useState([]),[err,setErr]=useState('');useEffect(()=>{api.audit().then(r=>setRows(r.data)).catch(e=>setErr(e.message))},[]);return <section><header className="page-head"><div><h1>Audit log</h1><p>Truy vết thao tác hệ thống</p></div></header>{err&&<div className="error">{err}</div>}<div className="table-wrap"><table><thead><tr><th>Thời gian</th><th>Người dùng</th><th>Vai trò</th><th>Hành động</th><th>Đối tượng</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.occurredAt).toLocaleString('vi-VN')}</td><td>{x.actorName}</td><td>{x.role}</td><td>{x.action}</td><td>{x.objectType} / {x.objectId}</td></tr>)}</tbody></table></div></section>}
