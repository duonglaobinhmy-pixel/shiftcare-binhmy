import { useState } from 'react';
import { Navigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
export default function Login(){const {user,login,loading}=useAuth();const [username,setUsername]=useState('admin'),[password,setPassword]=useState('Admin@123'),[err,setErr]=useState('');if(user)return <Navigate to="/" replace/>;async function submit(e){e.preventDefault();setErr('');try{await login(username,password)}catch(e){setErr(e.message)}}return <div className="login-page"><form className="login-card" onSubmit={submit}><img className="login-logo" src="/logo-binhmy.jpg" alt="Hệ thống dưỡng lão Bình Mỹ"/><div className="login-title"><h1>BÌNH MỸ CARE</h1><p>ShiftCare • Sổ giao ca & Nhật ký biến động NCT</p></div><label>Tài khoản<input autoFocus value={username} onChange={e=>setUsername(e.target.value)}/></label><label>Mật khẩu<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{err&&<div className="error">{err}</div>}<button disabled={loading}>{loading?'Đang đăng nhập...':'Đăng nhập'}</button><div className="demo-accounts"><b>Tài khoản demo</b><span>Admin: admin / Admin@123</span><span>Giám đốc: giamdoc.cuchi / Demo@123</span><span>CSV: csv.cuchi / Demo@123</span></div></form></div>}
