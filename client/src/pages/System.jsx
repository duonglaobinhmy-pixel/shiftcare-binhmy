import { useState } from 'react';import { api } from '../services/api';
export default function System(){const [d,setD]=useState(null),[loading,setLoading]=useState(false),[err,setErr]=useState('');async function test(){setLoading(true);setErr('');try{setD(await api.diagnostics())}catch(e){setErr(e.message)}finally{setLoading(false)}}return <section><header className="page-head"><div><h1>Kết nối BCARE</h1><p>Test login → /user/me → /elderly/getpaging</p></div><button onClick={test}>{loading?'Đang test...':'Test lại'}</button></header>{err&&<div className="error">{err}</div>}{d&&<div className="panel"><h2>{d.ok?'Kết nối thành công':'Kết nối chưa thành công'}</h2><p><b>Base URL:</b> {d.baseUrl}</p>{d.error&&<div className="error">{d.error}</div>}{(d.steps||[]).map((x,i)=><div className="diag" key={i}><b>{x.ok?'✓':'✗'} {x.step}</b><pre>{JSON.stringify(x,null,2)}</pre></div>)}</div>}</section>}
